--!strict
--!native
--!optimize 2
--!nolint LocalShadow
--!nolint LocalUnused
--!nolint FunctionUnused
--#selene: allow(shadowing)
-- File generated by Blink v0.18.8 (https://github.com/1Axen/Blink)
-- This file is not meant to be edited

_G._BLINK = _G._BLINK or {}
if _G._BLINK["BLINK"] then
	error("[Blink]: An instance of blink is already running with the remote scope \"BLINK\". Change the remote scope of either instance to avoid conflicts.")
end
_G._BLINK["BLINK"] = true
local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local RunService = game:GetService("RunService")

local SerdesFunctions = {}
local BASE_EVENT_NAME = "BLINK"
local Invocations = 0

local SendSize = 64
local SendOffset = 0
local SendCursor = 0
local SendBuffer = buffer.create(64)
local SendInstances = {}

local RecieveCursor = 0
local RecieveBuffer = buffer.create(64)

local RecieveInstances = {}
local RecieveInstanceCursor = 0

local Null = newproxy()

type Entry = {
    value: any,
    next: Entry?
}

type Queue = {
    head: Entry?,
    tail: Entry?
}

type BufferSave = {
    Size: number, 
    Cursor: number, 
    Buffer: buffer, 
    Instances: {Instance}
}

local function Read(Bytes: number)
    local Offset = RecieveCursor
    RecieveCursor += Bytes
    return Offset
end

local function Save(): BufferSave
    return {
        Size = SendSize,
        Cursor = SendCursor,
        Buffer = SendBuffer,
        Instances = SendInstances
    }
end

local function Load(Save: BufferSave?)
    if Save then
        SendSize = Save.Size
        SendCursor = Save.Cursor
        SendOffset = Save.Cursor
        SendBuffer = Save.Buffer
        SendInstances = Save.Instances
        return
    end

    SendSize = 64
    SendCursor = 0
    SendOffset = 0
    SendBuffer = buffer.create(64)
    SendInstances = {}
end

local function Invoke()
    if Invocations == 255 then
        Invocations = 0
    end

    local Invocation = Invocations
    Invocations += 1
    return Invocation
end

local function Allocate(Bytes: number)
    local InUse = (SendCursor + Bytes)
    if InUse > SendSize then
        --> Avoid resizing the buffer for every write
        while InUse > SendSize do
            SendSize *= 1.5
        end

        local Buffer = buffer.create(SendSize)
        buffer.copy(Buffer, 0, SendBuffer, 0, SendCursor)
        SendBuffer = Buffer
    end

    SendOffset = SendCursor
    SendCursor += Bytes
    
    return SendOffset
end

local function CreateQueue(): Queue
    return {
        head = nil,
        tail = nil
    }
end

local function Pop(queue: Queue): any
    local head = queue.head
    if head == nil then
        return
    end

    queue.head = head.next
    return head.value
end

local function Push(queue: Queue, value: any)
    local entry: Entry = {
        value = value,
        next = nil
    }

    if queue.tail ~= nil then
        queue.tail.next = entry
    end

    queue.tail = entry

    if queue.head == nil then
        queue.head = entry
    end
end

local Calls = table.create(256)

local Events: any = {
    Reliable = table.create(256),
    Unreliable = table.create(256)
}

local Queue: any = {
    Reliable = table.create(256),
    Unreliable = table.create(256)
}

local SerdesEvent = "?"
local SerdesThread: thread?
export type SmallFlatStruct = { id: number, x: number, y: number, z: number, active: boolean }
export type Leaf = { name: string, weight: number }
export type Third = { leaf: { name: string, weight: number }, flag: boolean }
export type Second = { inner: { leaf: { name: string, weight: number }, flag: boolean }, count: number }
export type First = { inner: { inner: { leaf: { name: string, weight: number }, flag: boolean }, count: number }, label: string }
export type NestedObject = { root: { inner: { inner: { leaf: { name: string, weight: number }, flag: boolean }, count: number }, label: string }, version: number }
export type WideStruct = { f1: number, f2: number, f3: number, f4: number, f5: number, f6: number, f7: number, f8: number, f9: number, f10: number, f11: number, f12: number, f13: number, f14: number, f15: number, f16: number, f17: number, f18: number, f19: number, f20: number, f21: number, f22: number, f23: number, f24: number, f25: number, f26: number, f27: number, f28: number, f29: number, f30: number, f31: number, f32: number, f33: number, f34: number, f35: number, f36: number, f37: number, f38: number, f39: number, f40: number, f41: number, f42: number, f43: number, f44: number, f45: number, f46: number, f47: number, f48: number, f49: number, f50: number }
export type LargeArray = { values: { number } }
export type Entries = {[string]: number}
export type LargeRecord = { entries: {[string]: number} }
export type StringHeavy = { title: string, author: string, lines: { string } }
export type Event = { kind: "spawn", id: number, at: Vector3 } | { kind: "damage", id: number, amount: number } | { kind: "chat", id: number, text: string } | { kind: "despawn", id: number }
export type TaggedUnion = { events: { { kind: "spawn", id: number, at: Vector3 } | { kind: "damage", id: number, amount: number } | { kind: "chat", id: number, text: string } | { kind: "despawn", id: number } } }
export type Toggles = { a: boolean, b: boolean, c: boolean, d: boolean, e: boolean, f: boolean, g: boolean, h: boolean, i: boolean, j: boolean, level: number, label: string?, offset: number? }
export type Transforms = { list: { CFrame } }
export type Booleans = { values: { boolean } }
export type Entity = { a: number, b: number, c: number, d: number, e: number, f: number }
export type Entities = { entities: { { a: number, b: number, c: number, d: number, e: number, f: number } } }
if not RunService:IsRunning() then
	local NOOP = function() end
	local Returns = table.freeze({
	SmallFlatStruct = {
		Read = function(Buffer: buffer): (SmallFlatStruct)
			-- Buffer overwrite
			local PreviousCursor = RecieveCursor
			local PreviousBuffer = RecieveBuffer
			local PreviousInstanceCursor = RecieveInstanceCursor
			RecieveCursor = 0
			RecieveBuffer = Buffer
			-- Read BLOCK: 17 bytes
			local BLOCK_START = Read(17)
			-- Buffer read
			local Value = {} :: any
			Value.id = buffer.readu32(RecieveBuffer, BLOCK_START + 0)
			Value.x = buffer.readf32(RecieveBuffer, BLOCK_START + 4)
			Value.y = buffer.readf32(RecieveBuffer, BLOCK_START + 8)
			Value.z = buffer.readf32(RecieveBuffer, BLOCK_START + 12)
			Value.active = (buffer.readu8(RecieveBuffer, BLOCK_START + 16) == 1)
			-- Buffer restore
			RecieveCursor = PreviousCursor
			RecieveBuffer = PreviousBuffer
			RecieveInstanceCursor = PreviousInstanceCursor
			return Value
		end,
		Write = function(Value: SmallFlatStruct): (buffer)
			-- Buffer overwrite
			local Previous = Save()
			Load()
			-- Allocate BLOCK: 17 bytes
			local BLOCK_START = Allocate(17)
			-- Buffer write
			buffer.writeu32(SendBuffer, BLOCK_START + 0, Value.id)
			buffer.writef32(SendBuffer, BLOCK_START + 4, Value.x)
			buffer.writef32(SendBuffer, BLOCK_START + 8, Value.y)
			buffer.writef32(SendBuffer, BLOCK_START + 12, Value.z)
			buffer.writeu8(SendBuffer, BLOCK_START + 16, Value.active and 1 or 0)
			-- Buffer restore
			local Result = buffer.create(SendCursor)
			buffer.copy(Result, 0, SendBuffer, 0, SendCursor)
			Load(Previous)
			return Result
		end
	},
	NestedObject = {
		Read = function(Buffer: buffer): (NestedObject)
			-- Buffer overwrite
			local PreviousCursor = RecieveCursor
			local PreviousBuffer = RecieveBuffer
			local PreviousInstanceCursor = RecieveInstanceCursor
			RecieveCursor = 0
			RecieveBuffer = Buffer
			-- Read BLOCK: 12 bytes
			local BLOCK_START = Read(12)
			-- Buffer read
			local Value = {} :: any
			Value.root = {} :: any
			Value.root.inner = {} :: any
			Value.root.inner.inner = {} :: any
			Value.root.inner.inner.leaf = {} :: any
			local Length = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
			Value.root.inner.inner.leaf.name = buffer.readstring(RecieveBuffer, Read(Length), Length)
			Value.root.inner.inner.leaf.weight = buffer.readf32(RecieveBuffer, BLOCK_START + 2)
			Value.root.inner.inner.flag = (buffer.readu8(RecieveBuffer, BLOCK_START + 6) == 1)
			Value.root.inner.count = buffer.readu16(RecieveBuffer, BLOCK_START + 7)
			Length = buffer.readu16(RecieveBuffer, BLOCK_START + 9)
			Value.root.label = buffer.readstring(RecieveBuffer, Read(Length), Length)
			Value.version = buffer.readu8(RecieveBuffer, BLOCK_START + 11)
			-- Buffer restore
			RecieveCursor = PreviousCursor
			RecieveBuffer = PreviousBuffer
			RecieveInstanceCursor = PreviousInstanceCursor
			return Value
		end,
		Write = function(Value: NestedObject): (buffer)
			-- Buffer overwrite
			local Previous = Save()
			Load()
			-- Allocate BLOCK: 12 bytes
			local BLOCK_START = Allocate(12)
			-- Buffer write
			local Length = #Value.root.inner.inner.leaf.name
			buffer.writeu16(SendBuffer, BLOCK_START + 0, Length)
			Allocate(Length)
			buffer.writestring(SendBuffer, SendOffset, Value.root.inner.inner.leaf.name, Length)
			buffer.writef32(SendBuffer, BLOCK_START + 2, Value.root.inner.inner.leaf.weight)
			buffer.writeu8(SendBuffer, BLOCK_START + 6, Value.root.inner.inner.flag and 1 or 0)
			buffer.writeu16(SendBuffer, BLOCK_START + 7, Value.root.inner.count)
			local Length = #Value.root.label
			buffer.writeu16(SendBuffer, BLOCK_START + 9, Length)
			Allocate(Length)
			buffer.writestring(SendBuffer, SendOffset, Value.root.label, Length)
			buffer.writeu8(SendBuffer, BLOCK_START + 11, Value.version)
			-- Buffer restore
			local Result = buffer.create(SendCursor)
			buffer.copy(Result, 0, SendBuffer, 0, SendCursor)
			Load(Previous)
			return Result
		end
	},
	WideStruct = {
		Read = function(Buffer: buffer): (WideStruct)
			-- Buffer overwrite
			local PreviousCursor = RecieveCursor
			local PreviousBuffer = RecieveBuffer
			local PreviousInstanceCursor = RecieveInstanceCursor
			RecieveCursor = 0
			RecieveBuffer = Buffer
			-- Read BLOCK: 200 bytes
			local BLOCK_START = Read(200)
			-- Buffer read
			local Value = {} :: any
			Value.f1 = buffer.readf32(RecieveBuffer, BLOCK_START + 0)
			Value.f2 = buffer.readf32(RecieveBuffer, BLOCK_START + 4)
			Value.f3 = buffer.readf32(RecieveBuffer, BLOCK_START + 8)
			Value.f4 = buffer.readf32(RecieveBuffer, BLOCK_START + 12)
			Value.f5 = buffer.readf32(RecieveBuffer, BLOCK_START + 16)
			Value.f6 = buffer.readf32(RecieveBuffer, BLOCK_START + 20)
			Value.f7 = buffer.readf32(RecieveBuffer, BLOCK_START + 24)
			Value.f8 = buffer.readf32(RecieveBuffer, BLOCK_START + 28)
			Value.f9 = buffer.readf32(RecieveBuffer, BLOCK_START + 32)
			Value.f10 = buffer.readf32(RecieveBuffer, BLOCK_START + 36)
			Value.f11 = buffer.readf32(RecieveBuffer, BLOCK_START + 40)
			Value.f12 = buffer.readf32(RecieveBuffer, BLOCK_START + 44)
			Value.f13 = buffer.readf32(RecieveBuffer, BLOCK_START + 48)
			Value.f14 = buffer.readf32(RecieveBuffer, BLOCK_START + 52)
			Value.f15 = buffer.readf32(RecieveBuffer, BLOCK_START + 56)
			Value.f16 = buffer.readf32(RecieveBuffer, BLOCK_START + 60)
			Value.f17 = buffer.readf32(RecieveBuffer, BLOCK_START + 64)
			Value.f18 = buffer.readf32(RecieveBuffer, BLOCK_START + 68)
			Value.f19 = buffer.readf32(RecieveBuffer, BLOCK_START + 72)
			Value.f20 = buffer.readf32(RecieveBuffer, BLOCK_START + 76)
			Value.f21 = buffer.readf32(RecieveBuffer, BLOCK_START + 80)
			Value.f22 = buffer.readf32(RecieveBuffer, BLOCK_START + 84)
			Value.f23 = buffer.readf32(RecieveBuffer, BLOCK_START + 88)
			Value.f24 = buffer.readf32(RecieveBuffer, BLOCK_START + 92)
			Value.f25 = buffer.readf32(RecieveBuffer, BLOCK_START + 96)
			Value.f26 = buffer.readf32(RecieveBuffer, BLOCK_START + 100)
			Value.f27 = buffer.readf32(RecieveBuffer, BLOCK_START + 104)
			Value.f28 = buffer.readf32(RecieveBuffer, BLOCK_START + 108)
			Value.f29 = buffer.readf32(RecieveBuffer, BLOCK_START + 112)
			Value.f30 = buffer.readf32(RecieveBuffer, BLOCK_START + 116)
			Value.f31 = buffer.readf32(RecieveBuffer, BLOCK_START + 120)
			Value.f32 = buffer.readf32(RecieveBuffer, BLOCK_START + 124)
			Value.f33 = buffer.readf32(RecieveBuffer, BLOCK_START + 128)
			Value.f34 = buffer.readf32(RecieveBuffer, BLOCK_START + 132)
			Value.f35 = buffer.readf32(RecieveBuffer, BLOCK_START + 136)
			Value.f36 = buffer.readf32(RecieveBuffer, BLOCK_START + 140)
			Value.f37 = buffer.readf32(RecieveBuffer, BLOCK_START + 144)
			Value.f38 = buffer.readf32(RecieveBuffer, BLOCK_START + 148)
			Value.f39 = buffer.readf32(RecieveBuffer, BLOCK_START + 152)
			Value.f40 = buffer.readf32(RecieveBuffer, BLOCK_START + 156)
			Value.f41 = buffer.readf32(RecieveBuffer, BLOCK_START + 160)
			Value.f42 = buffer.readf32(RecieveBuffer, BLOCK_START + 164)
			Value.f43 = buffer.readf32(RecieveBuffer, BLOCK_START + 168)
			Value.f44 = buffer.readf32(RecieveBuffer, BLOCK_START + 172)
			Value.f45 = buffer.readf32(RecieveBuffer, BLOCK_START + 176)
			Value.f46 = buffer.readf32(RecieveBuffer, BLOCK_START + 180)
			Value.f47 = buffer.readf32(RecieveBuffer, BLOCK_START + 184)
			Value.f48 = buffer.readf32(RecieveBuffer, BLOCK_START + 188)
			Value.f49 = buffer.readf32(RecieveBuffer, BLOCK_START + 192)
			Value.f50 = buffer.readf32(RecieveBuffer, BLOCK_START + 196)
			-- Buffer restore
			RecieveCursor = PreviousCursor
			RecieveBuffer = PreviousBuffer
			RecieveInstanceCursor = PreviousInstanceCursor
			return Value
		end,
		Write = function(Value: WideStruct): (buffer)
			-- Buffer overwrite
			local Previous = Save()
			Load()
			-- Allocate BLOCK: 200 bytes
			local BLOCK_START = Allocate(200)
			-- Buffer write
			buffer.writef32(SendBuffer, BLOCK_START + 0, Value.f1)
			buffer.writef32(SendBuffer, BLOCK_START + 4, Value.f2)
			buffer.writef32(SendBuffer, BLOCK_START + 8, Value.f3)
			buffer.writef32(SendBuffer, BLOCK_START + 12, Value.f4)
			buffer.writef32(SendBuffer, BLOCK_START + 16, Value.f5)
			buffer.writef32(SendBuffer, BLOCK_START + 20, Value.f6)
			buffer.writef32(SendBuffer, BLOCK_START + 24, Value.f7)
			buffer.writef32(SendBuffer, BLOCK_START + 28, Value.f8)
			buffer.writef32(SendBuffer, BLOCK_START + 32, Value.f9)
			buffer.writef32(SendBuffer, BLOCK_START + 36, Value.f10)
			buffer.writef32(SendBuffer, BLOCK_START + 40, Value.f11)
			buffer.writef32(SendBuffer, BLOCK_START + 44, Value.f12)
			buffer.writef32(SendBuffer, BLOCK_START + 48, Value.f13)
			buffer.writef32(SendBuffer, BLOCK_START + 52, Value.f14)
			buffer.writef32(SendBuffer, BLOCK_START + 56, Value.f15)
			buffer.writef32(SendBuffer, BLOCK_START + 60, Value.f16)
			buffer.writef32(SendBuffer, BLOCK_START + 64, Value.f17)
			buffer.writef32(SendBuffer, BLOCK_START + 68, Value.f18)
			buffer.writef32(SendBuffer, BLOCK_START + 72, Value.f19)
			buffer.writef32(SendBuffer, BLOCK_START + 76, Value.f20)
			buffer.writef32(SendBuffer, BLOCK_START + 80, Value.f21)
			buffer.writef32(SendBuffer, BLOCK_START + 84, Value.f22)
			buffer.writef32(SendBuffer, BLOCK_START + 88, Value.f23)
			buffer.writef32(SendBuffer, BLOCK_START + 92, Value.f24)
			buffer.writef32(SendBuffer, BLOCK_START + 96, Value.f25)
			buffer.writef32(SendBuffer, BLOCK_START + 100, Value.f26)
			buffer.writef32(SendBuffer, BLOCK_START + 104, Value.f27)
			buffer.writef32(SendBuffer, BLOCK_START + 108, Value.f28)
			buffer.writef32(SendBuffer, BLOCK_START + 112, Value.f29)
			buffer.writef32(SendBuffer, BLOCK_START + 116, Value.f30)
			buffer.writef32(SendBuffer, BLOCK_START + 120, Value.f31)
			buffer.writef32(SendBuffer, BLOCK_START + 124, Value.f32)
			buffer.writef32(SendBuffer, BLOCK_START + 128, Value.f33)
			buffer.writef32(SendBuffer, BLOCK_START + 132, Value.f34)
			buffer.writef32(SendBuffer, BLOCK_START + 136, Value.f35)
			buffer.writef32(SendBuffer, BLOCK_START + 140, Value.f36)
			buffer.writef32(SendBuffer, BLOCK_START + 144, Value.f37)
			buffer.writef32(SendBuffer, BLOCK_START + 148, Value.f38)
			buffer.writef32(SendBuffer, BLOCK_START + 152, Value.f39)
			buffer.writef32(SendBuffer, BLOCK_START + 156, Value.f40)
			buffer.writef32(SendBuffer, BLOCK_START + 160, Value.f41)
			buffer.writef32(SendBuffer, BLOCK_START + 164, Value.f42)
			buffer.writef32(SendBuffer, BLOCK_START + 168, Value.f43)
			buffer.writef32(SendBuffer, BLOCK_START + 172, Value.f44)
			buffer.writef32(SendBuffer, BLOCK_START + 176, Value.f45)
			buffer.writef32(SendBuffer, BLOCK_START + 180, Value.f46)
			buffer.writef32(SendBuffer, BLOCK_START + 184, Value.f47)
			buffer.writef32(SendBuffer, BLOCK_START + 188, Value.f48)
			buffer.writef32(SendBuffer, BLOCK_START + 192, Value.f49)
			buffer.writef32(SendBuffer, BLOCK_START + 196, Value.f50)
			-- Buffer restore
			local Result = buffer.create(SendCursor)
			buffer.copy(Result, 0, SendBuffer, 0, SendCursor)
			Load(Previous)
			return Result
		end
	},
	LargeArray = {
		Read = function(Buffer: buffer): (LargeArray)
			-- Buffer overwrite
			local PreviousCursor = RecieveCursor
			local PreviousBuffer = RecieveBuffer
			local PreviousInstanceCursor = RecieveInstanceCursor
			RecieveCursor = 0
			RecieveBuffer = Buffer
			-- Read BLOCK: 2 bytes
			local BLOCK_START = Read(2)
			-- Buffer read
			local Value = {} :: any
			-- START ARRAY
			local Length = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
			if Length < 0 then error(`Expected "Length" to be larger than 0, got {Length} instead.`) end
			if Length > 65535 then error(`Expected "Length" to be smaller than 65535, got {Length} instead.`) end
			Value.values = table.create(Length)
				-- Read BLOCK: 2 bytes
			local ARRAY_START_1 = Read(2 * Length)
			for Index = 1, Length do
				-- Read 2
				local OPERATION_OFFSET_0 = ARRAY_START_1
				ARRAY_START_1 += 2
				local Item_1 = buffer.readu16(RecieveBuffer, OPERATION_OFFSET_0)
				table.insert(Value.values, Item_1)
			end
			-- END ARRAY
			-- Buffer restore
			RecieveCursor = PreviousCursor
			RecieveBuffer = PreviousBuffer
			RecieveInstanceCursor = PreviousInstanceCursor
			return Value
		end,
		Write = function(Value: LargeArray): (buffer)
			-- Buffer overwrite
			local Previous = Save()
			Load()
			-- Allocate BLOCK: 2 bytes
			local BLOCK_START = Allocate(2)
			-- Buffer write
			-- START ARRAY
			buffer.writeu16(SendBuffer, BLOCK_START + 0, #Value.values)
				-- Allocate BLOCK: 2 bytes
			local ARRAY_START_1 = Allocate(2 * #Value.values)
			for Index = 1, #Value.values do
				local Item_1 = Value.values[Index]
				-- Allocate 2
				local OPERATION_OFFSET_0 = ARRAY_START_1
				ARRAY_START_1 += 2
				buffer.writeu16(SendBuffer, OPERATION_OFFSET_0, Item_1)
			end
			-- END ARRAY
			-- Buffer restore
			local Result = buffer.create(SendCursor)
			buffer.copy(Result, 0, SendBuffer, 0, SendCursor)
			Load(Previous)
			return Result
		end
	},
	LargeRecord = {
		Read = function(Buffer: buffer): (LargeRecord)
			-- Buffer overwrite
			local PreviousCursor = RecieveCursor
			local PreviousBuffer = RecieveBuffer
			local PreviousInstanceCursor = RecieveInstanceCursor
			RecieveCursor = 0
			RecieveBuffer = Buffer
			-- Read BLOCK: 2 bytes
			local BLOCK_START = Read(2)
			-- Buffer read
			local Value = {} :: any
			Value.entries = {}
			local Elements_1 = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
			for _ = 1, Elements_1 do
				local OFFSET_0 = Read(2)
				local Length = buffer.readu16(RecieveBuffer, OFFSET_0)
				local Key_1 = buffer.readstring(RecieveBuffer, Read(Length), Length)
				local OFFSET_1 = Read(1)
				local Element_1 = buffer.readu8(RecieveBuffer, OFFSET_1)
				Value.entries[Key_1] = Element_1
			end
			-- Buffer restore
			RecieveCursor = PreviousCursor
			RecieveBuffer = PreviousBuffer
			RecieveInstanceCursor = PreviousInstanceCursor
			return Value
		end,
		Write = function(Value: LargeRecord): (buffer)
			-- Buffer overwrite
			local Previous = Save()
			Load()
			-- Allocate BLOCK: 2 bytes
			local BLOCK_START = Allocate(2)
			-- Buffer write
			local Elements_1 = 0
			for Key_1, Element_1 in Value.entries do
				Elements_1 += 1
				local Length = #Key_1
				local OFFSET_0 = Allocate(2)
				buffer.writeu16(SendBuffer, OFFSET_0, Length)
				Allocate(Length)
				buffer.writestring(SendBuffer, SendOffset, Key_1, Length)
				local OFFSET_1 = Allocate(1)
				buffer.writeu8(SendBuffer, OFFSET_1, Element_1)
			end
			buffer.writeu16(SendBuffer, BLOCK_START + 0, Elements_1)
			-- Buffer restore
			local Result = buffer.create(SendCursor)
			buffer.copy(Result, 0, SendBuffer, 0, SendCursor)
			Load(Previous)
			return Result
		end
	},
	StringHeavy = {
		Read = function(Buffer: buffer): (StringHeavy)
			-- Buffer overwrite
			local PreviousCursor = RecieveCursor
			local PreviousBuffer = RecieveBuffer
			local PreviousInstanceCursor = RecieveInstanceCursor
			RecieveCursor = 0
			RecieveBuffer = Buffer
			-- Read BLOCK: 6 bytes
			local BLOCK_START = Read(6)
			-- Buffer read
			local Value = {} :: any
			local Length = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
			Value.title = buffer.readstring(RecieveBuffer, Read(Length), Length)
			Length = buffer.readu16(RecieveBuffer, BLOCK_START + 2)
			Value.author = buffer.readstring(RecieveBuffer, Read(Length), Length)
			-- START ARRAY
			Length = buffer.readu16(RecieveBuffer, BLOCK_START + 4)
			if Length < 0 then error(`Expected "Length" to be larger than 0, got {Length} instead.`) end
			if Length > 65535 then error(`Expected "Length" to be smaller than 65535, got {Length} instead.`) end
			Value.lines = table.create(Length)
				-- Read BLOCK: 2 bytes
			local ARRAY_START_1 = Read(2 * Length)
			for Index = 1, Length do
				-- Read 2
				local OPERATION_OFFSET_0 = ARRAY_START_1
				ARRAY_START_1 += 2
				Length = buffer.readu16(RecieveBuffer, OPERATION_OFFSET_0)
				local Item_1 = buffer.readstring(RecieveBuffer, Read(Length), Length)
				table.insert(Value.lines, Item_1)
			end
			-- END ARRAY
			-- Buffer restore
			RecieveCursor = PreviousCursor
			RecieveBuffer = PreviousBuffer
			RecieveInstanceCursor = PreviousInstanceCursor
			return Value
		end,
		Write = function(Value: StringHeavy): (buffer)
			-- Buffer overwrite
			local Previous = Save()
			Load()
			-- Allocate BLOCK: 6 bytes
			local BLOCK_START = Allocate(6)
			-- Buffer write
			local Length = #Value.title
			buffer.writeu16(SendBuffer, BLOCK_START + 0, Length)
			Allocate(Length)
			buffer.writestring(SendBuffer, SendOffset, Value.title, Length)
			local Length = #Value.author
			buffer.writeu16(SendBuffer, BLOCK_START + 2, Length)
			Allocate(Length)
			buffer.writestring(SendBuffer, SendOffset, Value.author, Length)
			-- START ARRAY
			buffer.writeu16(SendBuffer, BLOCK_START + 4, #Value.lines)
				-- Allocate BLOCK: 2 bytes
			local ARRAY_START_1 = Allocate(2 * #Value.lines)
			for Index = 1, #Value.lines do
				local Item_1 = Value.lines[Index]
				local Length = #Item_1
				-- Allocate 2
				local OPERATION_OFFSET_0 = ARRAY_START_1
				ARRAY_START_1 += 2
				buffer.writeu16(SendBuffer, OPERATION_OFFSET_0, Length)
				Allocate(Length)
				buffer.writestring(SendBuffer, SendOffset, Item_1, Length)
			end
			-- END ARRAY
			-- Buffer restore
			local Result = buffer.create(SendCursor)
			buffer.copy(Result, 0, SendBuffer, 0, SendCursor)
			Load(Previous)
			return Result
		end
	},
	TaggedUnion = {
		Read = function(Buffer: buffer): (TaggedUnion)
			-- Buffer overwrite
			local PreviousCursor = RecieveCursor
			local PreviousBuffer = RecieveBuffer
			local PreviousInstanceCursor = RecieveInstanceCursor
			RecieveCursor = 0
			RecieveBuffer = Buffer
			-- Read BLOCK: 2 bytes
			local BLOCK_START = Read(2)
			-- Buffer read
			local Value = {} :: any
			-- START ARRAY
			local Length = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
			if Length < 0 then error(`Expected "Length" to be larger than 0, got {Length} instead.`) end
			if Length > 65535 then error(`Expected "Length" to be smaller than 65535, got {Length} instead.`) end
			Value.events = table.create(Length)
				-- Read BLOCK: 1 bytes
			local ARRAY_START_1 = Read(1 * Length)
			for Index = 1, Length do
				-- Read 1
				local OPERATION_OFFSET_0 = ARRAY_START_1
				ARRAY_START_1 += 1
				local Tag = buffer.readu8(RecieveBuffer, OPERATION_OFFSET_0)
				local Item_1 = true :: any
				if Tag == 0 then
					-- Read BLOCK: 16 bytes
					local BLOCK_START = Read(16)
					Item_1 = {} :: any
					Item_1.id = buffer.readu32(RecieveBuffer, BLOCK_START + 0)
					local X = buffer.readf32(RecieveBuffer, BLOCK_START + 4)
					local Y = buffer.readf32(RecieveBuffer, BLOCK_START + 8)
					local Z = buffer.readf32(RecieveBuffer, BLOCK_START + 12)
					Item_1.at = Vector3.new(X, Y, Z)
					Item_1.kind = "spawn"
				elseif Tag == 1 then
					-- Read BLOCK: 6 bytes
					local BLOCK_START = Read(6)
					Item_1 = {} :: any
					Item_1.id = buffer.readu32(RecieveBuffer, BLOCK_START + 0)
					Item_1.amount = buffer.readu16(RecieveBuffer, BLOCK_START + 4)
					Item_1.kind = "damage"
				elseif Tag == 2 then
					-- Read BLOCK: 6 bytes
					local BLOCK_START = Read(6)
					Item_1 = {} :: any
					Item_1.id = buffer.readu32(RecieveBuffer, BLOCK_START + 0)
					Length = buffer.readu16(RecieveBuffer, BLOCK_START + 4)
					Item_1.text = buffer.readstring(RecieveBuffer, Read(Length), Length)
					Item_1.kind = "chat"
				elseif Tag == 3 then
					-- Read BLOCK: 4 bytes
					local BLOCK_START = Read(4)
					Item_1 = {} :: any
					Item_1.id = buffer.readu32(RecieveBuffer, BLOCK_START + 0)
					Item_1.kind = "despawn"
				else
					error(`Unexpected variant: {Tag}`)
				end
				table.insert(Value.events, Item_1)
			end
			-- END ARRAY
			-- Buffer restore
			RecieveCursor = PreviousCursor
			RecieveBuffer = PreviousBuffer
			RecieveInstanceCursor = PreviousInstanceCursor
			return Value
		end,
		Write = function(Value: TaggedUnion): (buffer)
			-- Buffer overwrite
			local Previous = Save()
			Load()
			-- Allocate BLOCK: 2 bytes
			local BLOCK_START = Allocate(2)
			-- Buffer write
			-- START ARRAY
			buffer.writeu16(SendBuffer, BLOCK_START + 0, #Value.events)
				-- Allocate BLOCK: 1 bytes
			local ARRAY_START_1 = Allocate(1 * #Value.events)
			for Index = 1, #Value.events do
				local Item_1 = Value.events[Index]
				-- Allocate 1
				local OPERATION_OFFSET_0 = ARRAY_START_1
				ARRAY_START_1 += 1
				if Item_1.kind == "spawn" then
					buffer.writeu8(SendBuffer, OPERATION_OFFSET_0, 0)
					-- Allocate BLOCK: 16 bytes
					local BLOCK_START = Allocate(16)
					buffer.writeu32(SendBuffer, BLOCK_START + 0, Item_1.id)
					local Vector = Item_1.at
					buffer.writef32(SendBuffer, BLOCK_START + 4, Vector.X)
					buffer.writef32(SendBuffer, BLOCK_START + 8, Vector.Y)
					buffer.writef32(SendBuffer, BLOCK_START + 12, Vector.Z)
				elseif Item_1.kind == "damage" then
					buffer.writeu8(SendBuffer, OPERATION_OFFSET_0, 1)
					-- Allocate BLOCK: 6 bytes
					local BLOCK_START = Allocate(6)
					buffer.writeu32(SendBuffer, BLOCK_START + 0, Item_1.id)
					buffer.writeu16(SendBuffer, BLOCK_START + 4, Item_1.amount)
				elseif Item_1.kind == "chat" then
					buffer.writeu8(SendBuffer, OPERATION_OFFSET_0, 2)
					-- Allocate BLOCK: 6 bytes
					local BLOCK_START = Allocate(6)
					buffer.writeu32(SendBuffer, BLOCK_START + 0, Item_1.id)
					local Length = #Item_1.text
					buffer.writeu16(SendBuffer, BLOCK_START + 4, Length)
					Allocate(Length)
					buffer.writestring(SendBuffer, SendOffset, Item_1.text, Length)
				elseif Item_1.kind == "despawn" then
					buffer.writeu8(SendBuffer, OPERATION_OFFSET_0, 3)
					-- Allocate BLOCK: 4 bytes
					local BLOCK_START = Allocate(4)
					buffer.writeu32(SendBuffer, BLOCK_START + 0, Item_1.id)
				else
					error(`Unexpected variant: {Item_1.kind}`)
				end
			end
			-- END ARRAY
			-- Buffer restore
			local Result = buffer.create(SendCursor)
			buffer.copy(Result, 0, SendBuffer, 0, SendCursor)
			Load(Previous)
			return Result
		end
	},
	Toggles = {
		Read = function(Buffer: buffer): (Toggles)
			-- Buffer overwrite
			local PreviousCursor = RecieveCursor
			local PreviousBuffer = RecieveBuffer
			local PreviousInstanceCursor = RecieveInstanceCursor
			RecieveCursor = 0
			RecieveBuffer = Buffer
			-- Read BLOCK: 13 bytes
			local BLOCK_START = Read(13)
			-- Buffer read
			local Value = {} :: any
			Value.a = (buffer.readu8(RecieveBuffer, BLOCK_START + 0) == 1)
			Value.b = (buffer.readu8(RecieveBuffer, BLOCK_START + 1) == 1)
			Value.c = (buffer.readu8(RecieveBuffer, BLOCK_START + 2) == 1)
			Value.d = (buffer.readu8(RecieveBuffer, BLOCK_START + 3) == 1)
			Value.e = (buffer.readu8(RecieveBuffer, BLOCK_START + 4) == 1)
			Value.f = (buffer.readu8(RecieveBuffer, BLOCK_START + 5) == 1)
			Value.g = (buffer.readu8(RecieveBuffer, BLOCK_START + 6) == 1)
			Value.h = (buffer.readu8(RecieveBuffer, BLOCK_START + 7) == 1)
			Value.i = (buffer.readu8(RecieveBuffer, BLOCK_START + 8) == 1)
			Value.j = (buffer.readu8(RecieveBuffer, BLOCK_START + 9) == 1)
			Value.level = buffer.readu8(RecieveBuffer, BLOCK_START + 10)
			if buffer.readu8(RecieveBuffer, BLOCK_START + 11) == 1 then
				-- Read BLOCK: 2 bytes
				local BLOCK_START = Read(2)
				local Length = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
				Value.label = buffer.readstring(RecieveBuffer, Read(Length), Length)
			end
			if buffer.readu8(RecieveBuffer, BLOCK_START + 12) == 1 then
				-- Read BLOCK: 2 bytes
				local BLOCK_START = Read(2)
				Value.offset = buffer.readi16(RecieveBuffer, BLOCK_START + 0)
			end
			-- Buffer restore
			RecieveCursor = PreviousCursor
			RecieveBuffer = PreviousBuffer
			RecieveInstanceCursor = PreviousInstanceCursor
			return Value
		end,
		Write = function(Value: Toggles): (buffer)
			-- Buffer overwrite
			local Previous = Save()
			Load()
			-- Allocate BLOCK: 13 bytes
			local BLOCK_START = Allocate(13)
			-- Buffer write
			buffer.writeu8(SendBuffer, BLOCK_START + 0, Value.a and 1 or 0)
			buffer.writeu8(SendBuffer, BLOCK_START + 1, Value.b and 1 or 0)
			buffer.writeu8(SendBuffer, BLOCK_START + 2, Value.c and 1 or 0)
			buffer.writeu8(SendBuffer, BLOCK_START + 3, Value.d and 1 or 0)
			buffer.writeu8(SendBuffer, BLOCK_START + 4, Value.e and 1 or 0)
			buffer.writeu8(SendBuffer, BLOCK_START + 5, Value.f and 1 or 0)
			buffer.writeu8(SendBuffer, BLOCK_START + 6, Value.g and 1 or 0)
			buffer.writeu8(SendBuffer, BLOCK_START + 7, Value.h and 1 or 0)
			buffer.writeu8(SendBuffer, BLOCK_START + 8, Value.i and 1 or 0)
			buffer.writeu8(SendBuffer, BLOCK_START + 9, Value.j and 1 or 0)
			buffer.writeu8(SendBuffer, BLOCK_START + 10, Value.level)
			buffer.writeu8(SendBuffer, BLOCK_START + 11, Value.label ~= nil and 1 or 0)
			if Value.label ~= nil then
				-- Allocate BLOCK: 2 bytes
				local BLOCK_START = Allocate(2)
				local Length = #Value.label
				buffer.writeu16(SendBuffer, BLOCK_START + 0, Length)
				Allocate(Length)
				buffer.writestring(SendBuffer, SendOffset, Value.label, Length)
			end
			buffer.writeu8(SendBuffer, BLOCK_START + 12, Value.offset ~= nil and 1 or 0)
			if Value.offset ~= nil then
				-- Allocate BLOCK: 2 bytes
				local BLOCK_START = Allocate(2)
				buffer.writei16(SendBuffer, BLOCK_START + 0, Value.offset)
			end
			-- Buffer restore
			local Result = buffer.create(SendCursor)
			buffer.copy(Result, 0, SendBuffer, 0, SendCursor)
			Load(Previous)
			return Result
		end
	},
	Transforms = {
		Read = function(Buffer: buffer): (Transforms)
			-- Buffer overwrite
			local PreviousCursor = RecieveCursor
			local PreviousBuffer = RecieveBuffer
			local PreviousInstanceCursor = RecieveInstanceCursor
			RecieveCursor = 0
			RecieveBuffer = Buffer
			-- Read BLOCK: 2 bytes
			local BLOCK_START = Read(2)
			-- Buffer read
			local Value = {} :: any
			-- START ARRAY
			local Length = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
			if Length < 0 then error(`Expected "Length" to be larger than 0, got {Length} instead.`) end
			if Length > 65535 then error(`Expected "Length" to be smaller than 65535, got {Length} instead.`) end
			Value.list = table.create(Length)
				-- Read BLOCK: 24 bytes
			local ARRAY_START_1 = Read(24 * Length)
			for Index = 1, Length do
				-- Read 4
				local OPERATION_OFFSET_0 = ARRAY_START_1
				ARRAY_START_1 += 4
				local X = buffer.readf32(RecieveBuffer, OPERATION_OFFSET_0)
				-- Read 4
				local OPERATION_OFFSET_1 = ARRAY_START_1
				ARRAY_START_1 += 4
				local Y = buffer.readf32(RecieveBuffer, OPERATION_OFFSET_1)
				-- Read 4
				local OPERATION_OFFSET_2 = ARRAY_START_1
				ARRAY_START_1 += 4
				local Z = buffer.readf32(RecieveBuffer, OPERATION_OFFSET_2)
				local Position = Vector3.new(X, Y, Z)
				-- Read 4
				local OPERATION_OFFSET_3 = ARRAY_START_1
				ARRAY_START_1 += 4
				local rX = buffer.readf32(RecieveBuffer, OPERATION_OFFSET_3)
				-- Read 4
				local OPERATION_OFFSET_4 = ARRAY_START_1
				ARRAY_START_1 += 4
				local rY = buffer.readf32(RecieveBuffer, OPERATION_OFFSET_4)
				-- Read 4
				local OPERATION_OFFSET_5 = ARRAY_START_1
				ARRAY_START_1 += 4
				local rZ = buffer.readf32(RecieveBuffer, OPERATION_OFFSET_5)
				local Item_1 = CFrame.new(Position) * CFrame.fromOrientation(rX, rY, rZ)
				table.insert(Value.list, Item_1)
			end
			-- END ARRAY
			-- Buffer restore
			RecieveCursor = PreviousCursor
			RecieveBuffer = PreviousBuffer
			RecieveInstanceCursor = PreviousInstanceCursor
			return Value
		end,
		Write = function(Value: Transforms): (buffer)
			-- Buffer overwrite
			local Previous = Save()
			Load()
			-- Allocate BLOCK: 2 bytes
			local BLOCK_START = Allocate(2)
			-- Buffer write
			-- START ARRAY
			buffer.writeu16(SendBuffer, BLOCK_START + 0, #Value.list)
				-- Allocate BLOCK: 24 bytes
			local ARRAY_START_1 = Allocate(24 * #Value.list)
			for Index = 1, #Value.list do
				local Item_1 = Value.list[Index]
				local Vector = Item_1.Position
				-- Allocate 4
				local OPERATION_OFFSET_0 = ARRAY_START_1
				ARRAY_START_1 += 4
				buffer.writef32(SendBuffer, OPERATION_OFFSET_0, Vector.X)
				-- Allocate 4
				local OPERATION_OFFSET_1 = ARRAY_START_1
				ARRAY_START_1 += 4
				buffer.writef32(SendBuffer, OPERATION_OFFSET_1, Vector.Y)
				-- Allocate 4
				local OPERATION_OFFSET_2 = ARRAY_START_1
				ARRAY_START_1 += 4
				buffer.writef32(SendBuffer, OPERATION_OFFSET_2, Vector.Z)
				local rX, rY, rZ = Item_1:ToOrientation()
				-- Allocate 4
				local OPERATION_OFFSET_3 = ARRAY_START_1
				ARRAY_START_1 += 4
				buffer.writef32(SendBuffer, OPERATION_OFFSET_3, rX)
				-- Allocate 4
				local OPERATION_OFFSET_4 = ARRAY_START_1
				ARRAY_START_1 += 4
				buffer.writef32(SendBuffer, OPERATION_OFFSET_4, rY)
				-- Allocate 4
				local OPERATION_OFFSET_5 = ARRAY_START_1
				ARRAY_START_1 += 4
				buffer.writef32(SendBuffer, OPERATION_OFFSET_5, rZ)
			end
			-- END ARRAY
			-- Buffer restore
			local Result = buffer.create(SendCursor)
			buffer.copy(Result, 0, SendBuffer, 0, SendCursor)
			Load(Previous)
			return Result
		end
	},
	Booleans = {
		Read = function(Buffer: buffer): (Booleans)
			-- Buffer overwrite
			local PreviousCursor = RecieveCursor
			local PreviousBuffer = RecieveBuffer
			local PreviousInstanceCursor = RecieveInstanceCursor
			RecieveCursor = 0
			RecieveBuffer = Buffer
			-- Read BLOCK: 2 bytes
			local BLOCK_START = Read(2)
			-- Buffer read
			local Value = {} :: any
			-- START ARRAY
			local Length = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
			if Length < 0 then error(`Expected "Length" to be larger than 0, got {Length} instead.`) end
			if Length > 1000 then error(`Expected "Length" to be smaller than 1000, got {Length} instead.`) end
			Value.values = table.create(Length)
				-- Read BLOCK: 1 bytes
			local ARRAY_START_1 = Read(1 * Length)
			for Index = 1, Length do
				-- Read 1
				local OPERATION_OFFSET_0 = ARRAY_START_1
				ARRAY_START_1 += 1
				local Item_1 = (buffer.readu8(RecieveBuffer, OPERATION_OFFSET_0) == 1)
				table.insert(Value.values, Item_1)
			end
			-- END ARRAY
			-- Buffer restore
			RecieveCursor = PreviousCursor
			RecieveBuffer = PreviousBuffer
			RecieveInstanceCursor = PreviousInstanceCursor
			return Value
		end,
		Write = function(Value: Booleans): (buffer)
			-- Buffer overwrite
			local Previous = Save()
			Load()
			-- Allocate BLOCK: 2 bytes
			local BLOCK_START = Allocate(2)
			-- Buffer write
			-- START ARRAY
			buffer.writeu16(SendBuffer, BLOCK_START + 0, #Value.values)
				-- Allocate BLOCK: 1 bytes
			local ARRAY_START_1 = Allocate(1 * #Value.values)
			for Index = 1, #Value.values do
				local Item_1 = Value.values[Index]
				-- Allocate 1
				local OPERATION_OFFSET_0 = ARRAY_START_1
				ARRAY_START_1 += 1
				buffer.writeu8(SendBuffer, OPERATION_OFFSET_0, Item_1 and 1 or 0)
			end
			-- END ARRAY
			-- Buffer restore
			local Result = buffer.create(SendCursor)
			buffer.copy(Result, 0, SendBuffer, 0, SendCursor)
			Load(Previous)
			return Result
		end
	},
	Entities = {
		Read = function(Buffer: buffer): (Entities)
			-- Buffer overwrite
			local PreviousCursor = RecieveCursor
			local PreviousBuffer = RecieveBuffer
			local PreviousInstanceCursor = RecieveInstanceCursor
			RecieveCursor = 0
			RecieveBuffer = Buffer
			-- Read BLOCK: 2 bytes
			local BLOCK_START = Read(2)
			-- Buffer read
			local Value = {} :: any
			-- START ARRAY
			local Length = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
			if Length < 0 then error(`Expected "Length" to be larger than 0, got {Length} instead.`) end
			if Length > 1000 then error(`Expected "Length" to be smaller than 1000, got {Length} instead.`) end
			Value.entities = table.create(Length)
				-- Read BLOCK: 6 bytes
			local ARRAY_START_1 = Read(6 * Length)
			for Index = 1, Length do
				local Item_1 = {} :: any
				-- Read 1
				local OPERATION_OFFSET_0 = ARRAY_START_1
				ARRAY_START_1 += 1
				Item_1.a = buffer.readu8(RecieveBuffer, OPERATION_OFFSET_0)
				-- Read 1
				local OPERATION_OFFSET_1 = ARRAY_START_1
				ARRAY_START_1 += 1
				Item_1.b = buffer.readu8(RecieveBuffer, OPERATION_OFFSET_1)
				-- Read 1
				local OPERATION_OFFSET_2 = ARRAY_START_1
				ARRAY_START_1 += 1
				Item_1.c = buffer.readu8(RecieveBuffer, OPERATION_OFFSET_2)
				-- Read 1
				local OPERATION_OFFSET_3 = ARRAY_START_1
				ARRAY_START_1 += 1
				Item_1.d = buffer.readu8(RecieveBuffer, OPERATION_OFFSET_3)
				-- Read 1
				local OPERATION_OFFSET_4 = ARRAY_START_1
				ARRAY_START_1 += 1
				Item_1.e = buffer.readu8(RecieveBuffer, OPERATION_OFFSET_4)
				-- Read 1
				local OPERATION_OFFSET_5 = ARRAY_START_1
				ARRAY_START_1 += 1
				Item_1.f = buffer.readu8(RecieveBuffer, OPERATION_OFFSET_5)
				table.insert(Value.entities, Item_1)
			end
			-- END ARRAY
			-- Buffer restore
			RecieveCursor = PreviousCursor
			RecieveBuffer = PreviousBuffer
			RecieveInstanceCursor = PreviousInstanceCursor
			return Value
		end,
		Write = function(Value: Entities): (buffer)
			-- Buffer overwrite
			local Previous = Save()
			Load()
			-- Allocate BLOCK: 2 bytes
			local BLOCK_START = Allocate(2)
			-- Buffer write
			-- START ARRAY
			buffer.writeu16(SendBuffer, BLOCK_START + 0, #Value.entities)
				-- Allocate BLOCK: 6 bytes
			local ARRAY_START_1 = Allocate(6 * #Value.entities)
			for Index = 1, #Value.entities do
				local Item_1 = Value.entities[Index]
				-- Allocate 1
				local OPERATION_OFFSET_0 = ARRAY_START_1
				ARRAY_START_1 += 1
				buffer.writeu8(SendBuffer, OPERATION_OFFSET_0, Item_1.a)
				-- Allocate 1
				local OPERATION_OFFSET_1 = ARRAY_START_1
				ARRAY_START_1 += 1
				buffer.writeu8(SendBuffer, OPERATION_OFFSET_1, Item_1.b)
				-- Allocate 1
				local OPERATION_OFFSET_2 = ARRAY_START_1
				ARRAY_START_1 += 1
				buffer.writeu8(SendBuffer, OPERATION_OFFSET_2, Item_1.c)
				-- Allocate 1
				local OPERATION_OFFSET_3 = ARRAY_START_1
				ARRAY_START_1 += 1
				buffer.writeu8(SendBuffer, OPERATION_OFFSET_3, Item_1.d)
				-- Allocate 1
				local OPERATION_OFFSET_4 = ARRAY_START_1
				ARRAY_START_1 += 1
				buffer.writeu8(SendBuffer, OPERATION_OFFSET_4, Item_1.e)
				-- Allocate 1
				local OPERATION_OFFSET_5 = ARRAY_START_1
				ARRAY_START_1 += 1
				buffer.writeu8(SendBuffer, OPERATION_OFFSET_5, Item_1.f)
			end
			-- END ARRAY
			-- Buffer restore
			local Result = buffer.create(SendCursor)
			buffer.copy(Result, 0, SendBuffer, 0, SendCursor)
			Load(Previous)
			return Result
		end
	},
	})
	return Returns :: BLINK_EVENTS_SYMBOL
end

if not RunService:IsServer() then
    error("Server network module can only be required from the server.")
end

local Reliable: RemoteEvent = ReplicatedStorage:FindFirstChild(BASE_EVENT_NAME .. "_RELIABLE_REMOTE") :: RemoteEvent
if not Reliable then
    local RemoteEvent = Instance.new("RemoteEvent")
    RemoteEvent.Name = BASE_EVENT_NAME .. "_RELIABLE_REMOTE"
    RemoteEvent.Parent = ReplicatedStorage
    Reliable = RemoteEvent
end

local Unreliable: UnreliableRemoteEvent = ReplicatedStorage:FindFirstChild(BASE_EVENT_NAME .. "_UNRELIABLE_REMOTE") :: UnreliableRemoteEvent
if not Unreliable then
    local UnreliableRemoteEvent = Instance.new("UnreliableRemoteEvent")
    UnreliableRemoteEvent.Name = BASE_EVENT_NAME .. "_UNRELIABLE_REMOTE"
    UnreliableRemoteEvent.Parent = ReplicatedStorage
    Unreliable = UnreliableRemoteEvent
end

local PlayersMap: {[Player]: BufferSave} = {}

Players.PlayerRemoving:Connect(function(Player)
    PlayersMap[Player] = nil
end)

local function StepReplication()
    for Player, Send in PlayersMap do
        if Send.Cursor <= 0 then
            continue
        end

        local Buffer = buffer.create(Send.Cursor)
        buffer.copy(Buffer, 0, Send.Buffer, 0, Send.Cursor)
        Reliable:FireClient(Player, Buffer, Send.Instances)

        Send.Size = 64
        Send.Cursor = 0
        Send.Buffer = buffer.create(64)
        table.clear(Send.Instances)
    end
end
Reliable.OnServerEvent:Connect(function(Player: Player, Buffer: buffer, Instances: {Instance})
	RecieveCursor = 0
	RecieveBuffer = Buffer
	RecieveInstances = Instances
	RecieveInstanceCursor = 0
	local Size = buffer.len(RecieveBuffer)
	if SerdesThread and coroutine.status(SerdesThread) == "suspended" then
		warn(`[Blink]: Yielded in a Sync call. Event "{SerdesEvent}" uses a Sync call, consider using an Async call instead.`)
		coroutine.close(SerdesThread)
	end
	SerdesThread = coroutine.running()
	while (RecieveCursor < Size) do
		-- Read BLOCK: 1 bytes
		local BLOCK_START = Read(1)
		local Index = buffer.readu8(RecieveBuffer, BLOCK_START + 0)
	end
end)
Unreliable.OnServerEvent:Connect(function(Player: Player, Buffer: buffer, Instances: {Instance})
	RecieveCursor = 0
	RecieveBuffer = Buffer
	RecieveInstances = Instances
	RecieveInstanceCursor = 0
	local Size = buffer.len(RecieveBuffer)
	if SerdesThread and coroutine.status(SerdesThread) == "suspended" then
		warn(`[Blink]: Yielded in a Sync call. Event "{SerdesEvent}" uses a Sync call, consider using an Async call instead.`)
		coroutine.close(SerdesThread)
	end
	SerdesThread = coroutine.running()
	while (RecieveCursor < Size) do
		-- Read BLOCK: 1 bytes
		local BLOCK_START = Read(1)
		local Index = buffer.readu8(RecieveBuffer, BLOCK_START + 0)
	end
end)


local Returns = table.freeze({
	StepReplication = StepReplication,

	SmallFlatStruct = {
		Read = function(Buffer: buffer): (SmallFlatStruct)
			-- Buffer overwrite
			local PreviousCursor = RecieveCursor
			local PreviousBuffer = RecieveBuffer
			local PreviousInstanceCursor = RecieveInstanceCursor
			RecieveCursor = 0
			RecieveBuffer = Buffer
			-- Read BLOCK: 17 bytes
			local BLOCK_START = Read(17)
			-- Buffer read
			local Value = {} :: any
			Value.id = buffer.readu32(RecieveBuffer, BLOCK_START + 0)
			Value.x = buffer.readf32(RecieveBuffer, BLOCK_START + 4)
			Value.y = buffer.readf32(RecieveBuffer, BLOCK_START + 8)
			Value.z = buffer.readf32(RecieveBuffer, BLOCK_START + 12)
			Value.active = (buffer.readu8(RecieveBuffer, BLOCK_START + 16) == 1)
			-- Buffer restore
			RecieveCursor = PreviousCursor
			RecieveBuffer = PreviousBuffer
			RecieveInstanceCursor = PreviousInstanceCursor
			return Value
		end,
		Write = function(Value: SmallFlatStruct): (buffer)
			-- Buffer overwrite
			local Previous = Save()
			Load()
			-- Allocate BLOCK: 17 bytes
			local BLOCK_START = Allocate(17)
			-- Buffer write
			buffer.writeu32(SendBuffer, BLOCK_START + 0, Value.id)
			buffer.writef32(SendBuffer, BLOCK_START + 4, Value.x)
			buffer.writef32(SendBuffer, BLOCK_START + 8, Value.y)
			buffer.writef32(SendBuffer, BLOCK_START + 12, Value.z)
			buffer.writeu8(SendBuffer, BLOCK_START + 16, Value.active and 1 or 0)
			-- Buffer restore
			local Result = buffer.create(SendCursor)
			buffer.copy(Result, 0, SendBuffer, 0, SendCursor)
			Load(Previous)
			return Result
		end
	},
	NestedObject = {
		Read = function(Buffer: buffer): (NestedObject)
			-- Buffer overwrite
			local PreviousCursor = RecieveCursor
			local PreviousBuffer = RecieveBuffer
			local PreviousInstanceCursor = RecieveInstanceCursor
			RecieveCursor = 0
			RecieveBuffer = Buffer
			-- Read BLOCK: 12 bytes
			local BLOCK_START = Read(12)
			-- Buffer read
			local Value = {} :: any
			Value.root = {} :: any
			Value.root.inner = {} :: any
			Value.root.inner.inner = {} :: any
			Value.root.inner.inner.leaf = {} :: any
			local Length = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
			Value.root.inner.inner.leaf.name = buffer.readstring(RecieveBuffer, Read(Length), Length)
			Value.root.inner.inner.leaf.weight = buffer.readf32(RecieveBuffer, BLOCK_START + 2)
			Value.root.inner.inner.flag = (buffer.readu8(RecieveBuffer, BLOCK_START + 6) == 1)
			Value.root.inner.count = buffer.readu16(RecieveBuffer, BLOCK_START + 7)
			Length = buffer.readu16(RecieveBuffer, BLOCK_START + 9)
			Value.root.label = buffer.readstring(RecieveBuffer, Read(Length), Length)
			Value.version = buffer.readu8(RecieveBuffer, BLOCK_START + 11)
			-- Buffer restore
			RecieveCursor = PreviousCursor
			RecieveBuffer = PreviousBuffer
			RecieveInstanceCursor = PreviousInstanceCursor
			return Value
		end,
		Write = function(Value: NestedObject): (buffer)
			-- Buffer overwrite
			local Previous = Save()
			Load()
			-- Allocate BLOCK: 12 bytes
			local BLOCK_START = Allocate(12)
			-- Buffer write
			local Length = #Value.root.inner.inner.leaf.name
			buffer.writeu16(SendBuffer, BLOCK_START + 0, Length)
			Allocate(Length)
			buffer.writestring(SendBuffer, SendOffset, Value.root.inner.inner.leaf.name, Length)
			buffer.writef32(SendBuffer, BLOCK_START + 2, Value.root.inner.inner.leaf.weight)
			buffer.writeu8(SendBuffer, BLOCK_START + 6, Value.root.inner.inner.flag and 1 or 0)
			buffer.writeu16(SendBuffer, BLOCK_START + 7, Value.root.inner.count)
			local Length = #Value.root.label
			buffer.writeu16(SendBuffer, BLOCK_START + 9, Length)
			Allocate(Length)
			buffer.writestring(SendBuffer, SendOffset, Value.root.label, Length)
			buffer.writeu8(SendBuffer, BLOCK_START + 11, Value.version)
			-- Buffer restore
			local Result = buffer.create(SendCursor)
			buffer.copy(Result, 0, SendBuffer, 0, SendCursor)
			Load(Previous)
			return Result
		end
	},
	WideStruct = {
		Read = function(Buffer: buffer): (WideStruct)
			-- Buffer overwrite
			local PreviousCursor = RecieveCursor
			local PreviousBuffer = RecieveBuffer
			local PreviousInstanceCursor = RecieveInstanceCursor
			RecieveCursor = 0
			RecieveBuffer = Buffer
			-- Read BLOCK: 200 bytes
			local BLOCK_START = Read(200)
			-- Buffer read
			local Value = {} :: any
			Value.f1 = buffer.readf32(RecieveBuffer, BLOCK_START + 0)
			Value.f2 = buffer.readf32(RecieveBuffer, BLOCK_START + 4)
			Value.f3 = buffer.readf32(RecieveBuffer, BLOCK_START + 8)
			Value.f4 = buffer.readf32(RecieveBuffer, BLOCK_START + 12)
			Value.f5 = buffer.readf32(RecieveBuffer, BLOCK_START + 16)
			Value.f6 = buffer.readf32(RecieveBuffer, BLOCK_START + 20)
			Value.f7 = buffer.readf32(RecieveBuffer, BLOCK_START + 24)
			Value.f8 = buffer.readf32(RecieveBuffer, BLOCK_START + 28)
			Value.f9 = buffer.readf32(RecieveBuffer, BLOCK_START + 32)
			Value.f10 = buffer.readf32(RecieveBuffer, BLOCK_START + 36)
			Value.f11 = buffer.readf32(RecieveBuffer, BLOCK_START + 40)
			Value.f12 = buffer.readf32(RecieveBuffer, BLOCK_START + 44)
			Value.f13 = buffer.readf32(RecieveBuffer, BLOCK_START + 48)
			Value.f14 = buffer.readf32(RecieveBuffer, BLOCK_START + 52)
			Value.f15 = buffer.readf32(RecieveBuffer, BLOCK_START + 56)
			Value.f16 = buffer.readf32(RecieveBuffer, BLOCK_START + 60)
			Value.f17 = buffer.readf32(RecieveBuffer, BLOCK_START + 64)
			Value.f18 = buffer.readf32(RecieveBuffer, BLOCK_START + 68)
			Value.f19 = buffer.readf32(RecieveBuffer, BLOCK_START + 72)
			Value.f20 = buffer.readf32(RecieveBuffer, BLOCK_START + 76)
			Value.f21 = buffer.readf32(RecieveBuffer, BLOCK_START + 80)
			Value.f22 = buffer.readf32(RecieveBuffer, BLOCK_START + 84)
			Value.f23 = buffer.readf32(RecieveBuffer, BLOCK_START + 88)
			Value.f24 = buffer.readf32(RecieveBuffer, BLOCK_START + 92)
			Value.f25 = buffer.readf32(RecieveBuffer, BLOCK_START + 96)
			Value.f26 = buffer.readf32(RecieveBuffer, BLOCK_START + 100)
			Value.f27 = buffer.readf32(RecieveBuffer, BLOCK_START + 104)
			Value.f28 = buffer.readf32(RecieveBuffer, BLOCK_START + 108)
			Value.f29 = buffer.readf32(RecieveBuffer, BLOCK_START + 112)
			Value.f30 = buffer.readf32(RecieveBuffer, BLOCK_START + 116)
			Value.f31 = buffer.readf32(RecieveBuffer, BLOCK_START + 120)
			Value.f32 = buffer.readf32(RecieveBuffer, BLOCK_START + 124)
			Value.f33 = buffer.readf32(RecieveBuffer, BLOCK_START + 128)
			Value.f34 = buffer.readf32(RecieveBuffer, BLOCK_START + 132)
			Value.f35 = buffer.readf32(RecieveBuffer, BLOCK_START + 136)
			Value.f36 = buffer.readf32(RecieveBuffer, BLOCK_START + 140)
			Value.f37 = buffer.readf32(RecieveBuffer, BLOCK_START + 144)
			Value.f38 = buffer.readf32(RecieveBuffer, BLOCK_START + 148)
			Value.f39 = buffer.readf32(RecieveBuffer, BLOCK_START + 152)
			Value.f40 = buffer.readf32(RecieveBuffer, BLOCK_START + 156)
			Value.f41 = buffer.readf32(RecieveBuffer, BLOCK_START + 160)
			Value.f42 = buffer.readf32(RecieveBuffer, BLOCK_START + 164)
			Value.f43 = buffer.readf32(RecieveBuffer, BLOCK_START + 168)
			Value.f44 = buffer.readf32(RecieveBuffer, BLOCK_START + 172)
			Value.f45 = buffer.readf32(RecieveBuffer, BLOCK_START + 176)
			Value.f46 = buffer.readf32(RecieveBuffer, BLOCK_START + 180)
			Value.f47 = buffer.readf32(RecieveBuffer, BLOCK_START + 184)
			Value.f48 = buffer.readf32(RecieveBuffer, BLOCK_START + 188)
			Value.f49 = buffer.readf32(RecieveBuffer, BLOCK_START + 192)
			Value.f50 = buffer.readf32(RecieveBuffer, BLOCK_START + 196)
			-- Buffer restore
			RecieveCursor = PreviousCursor
			RecieveBuffer = PreviousBuffer
			RecieveInstanceCursor = PreviousInstanceCursor
			return Value
		end,
		Write = function(Value: WideStruct): (buffer)
			-- Buffer overwrite
			local Previous = Save()
			Load()
			-- Allocate BLOCK: 200 bytes
			local BLOCK_START = Allocate(200)
			-- Buffer write
			buffer.writef32(SendBuffer, BLOCK_START + 0, Value.f1)
			buffer.writef32(SendBuffer, BLOCK_START + 4, Value.f2)
			buffer.writef32(SendBuffer, BLOCK_START + 8, Value.f3)
			buffer.writef32(SendBuffer, BLOCK_START + 12, Value.f4)
			buffer.writef32(SendBuffer, BLOCK_START + 16, Value.f5)
			buffer.writef32(SendBuffer, BLOCK_START + 20, Value.f6)
			buffer.writef32(SendBuffer, BLOCK_START + 24, Value.f7)
			buffer.writef32(SendBuffer, BLOCK_START + 28, Value.f8)
			buffer.writef32(SendBuffer, BLOCK_START + 32, Value.f9)
			buffer.writef32(SendBuffer, BLOCK_START + 36, Value.f10)
			buffer.writef32(SendBuffer, BLOCK_START + 40, Value.f11)
			buffer.writef32(SendBuffer, BLOCK_START + 44, Value.f12)
			buffer.writef32(SendBuffer, BLOCK_START + 48, Value.f13)
			buffer.writef32(SendBuffer, BLOCK_START + 52, Value.f14)
			buffer.writef32(SendBuffer, BLOCK_START + 56, Value.f15)
			buffer.writef32(SendBuffer, BLOCK_START + 60, Value.f16)
			buffer.writef32(SendBuffer, BLOCK_START + 64, Value.f17)
			buffer.writef32(SendBuffer, BLOCK_START + 68, Value.f18)
			buffer.writef32(SendBuffer, BLOCK_START + 72, Value.f19)
			buffer.writef32(SendBuffer, BLOCK_START + 76, Value.f20)
			buffer.writef32(SendBuffer, BLOCK_START + 80, Value.f21)
			buffer.writef32(SendBuffer, BLOCK_START + 84, Value.f22)
			buffer.writef32(SendBuffer, BLOCK_START + 88, Value.f23)
			buffer.writef32(SendBuffer, BLOCK_START + 92, Value.f24)
			buffer.writef32(SendBuffer, BLOCK_START + 96, Value.f25)
			buffer.writef32(SendBuffer, BLOCK_START + 100, Value.f26)
			buffer.writef32(SendBuffer, BLOCK_START + 104, Value.f27)
			buffer.writef32(SendBuffer, BLOCK_START + 108, Value.f28)
			buffer.writef32(SendBuffer, BLOCK_START + 112, Value.f29)
			buffer.writef32(SendBuffer, BLOCK_START + 116, Value.f30)
			buffer.writef32(SendBuffer, BLOCK_START + 120, Value.f31)
			buffer.writef32(SendBuffer, BLOCK_START + 124, Value.f32)
			buffer.writef32(SendBuffer, BLOCK_START + 128, Value.f33)
			buffer.writef32(SendBuffer, BLOCK_START + 132, Value.f34)
			buffer.writef32(SendBuffer, BLOCK_START + 136, Value.f35)
			buffer.writef32(SendBuffer, BLOCK_START + 140, Value.f36)
			buffer.writef32(SendBuffer, BLOCK_START + 144, Value.f37)
			buffer.writef32(SendBuffer, BLOCK_START + 148, Value.f38)
			buffer.writef32(SendBuffer, BLOCK_START + 152, Value.f39)
			buffer.writef32(SendBuffer, BLOCK_START + 156, Value.f40)
			buffer.writef32(SendBuffer, BLOCK_START + 160, Value.f41)
			buffer.writef32(SendBuffer, BLOCK_START + 164, Value.f42)
			buffer.writef32(SendBuffer, BLOCK_START + 168, Value.f43)
			buffer.writef32(SendBuffer, BLOCK_START + 172, Value.f44)
			buffer.writef32(SendBuffer, BLOCK_START + 176, Value.f45)
			buffer.writef32(SendBuffer, BLOCK_START + 180, Value.f46)
			buffer.writef32(SendBuffer, BLOCK_START + 184, Value.f47)
			buffer.writef32(SendBuffer, BLOCK_START + 188, Value.f48)
			buffer.writef32(SendBuffer, BLOCK_START + 192, Value.f49)
			buffer.writef32(SendBuffer, BLOCK_START + 196, Value.f50)
			-- Buffer restore
			local Result = buffer.create(SendCursor)
			buffer.copy(Result, 0, SendBuffer, 0, SendCursor)
			Load(Previous)
			return Result
		end
	},
	LargeArray = {
		Read = function(Buffer: buffer): (LargeArray)
			-- Buffer overwrite
			local PreviousCursor = RecieveCursor
			local PreviousBuffer = RecieveBuffer
			local PreviousInstanceCursor = RecieveInstanceCursor
			RecieveCursor = 0
			RecieveBuffer = Buffer
			-- Read BLOCK: 2 bytes
			local BLOCK_START = Read(2)
			-- Buffer read
			local Value = {} :: any
			-- START ARRAY
			local Length = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
			if Length < 0 then error(`Expected "Length" to be larger than 0, got {Length} instead.`) end
			if Length > 65535 then error(`Expected "Length" to be smaller than 65535, got {Length} instead.`) end
			Value.values = table.create(Length)
				-- Read BLOCK: 2 bytes
			local ARRAY_START_1 = Read(2 * Length)
			for Index = 1, Length do
				-- Read 2
				local OPERATION_OFFSET_0 = ARRAY_START_1
				ARRAY_START_1 += 2
				local Item_1 = buffer.readu16(RecieveBuffer, OPERATION_OFFSET_0)
				table.insert(Value.values, Item_1)
			end
			-- END ARRAY
			-- Buffer restore
			RecieveCursor = PreviousCursor
			RecieveBuffer = PreviousBuffer
			RecieveInstanceCursor = PreviousInstanceCursor
			return Value
		end,
		Write = function(Value: LargeArray): (buffer)
			-- Buffer overwrite
			local Previous = Save()
			Load()
			-- Allocate BLOCK: 2 bytes
			local BLOCK_START = Allocate(2)
			-- Buffer write
			-- START ARRAY
			buffer.writeu16(SendBuffer, BLOCK_START + 0, #Value.values)
				-- Allocate BLOCK: 2 bytes
			local ARRAY_START_1 = Allocate(2 * #Value.values)
			for Index = 1, #Value.values do
				local Item_1 = Value.values[Index]
				-- Allocate 2
				local OPERATION_OFFSET_0 = ARRAY_START_1
				ARRAY_START_1 += 2
				buffer.writeu16(SendBuffer, OPERATION_OFFSET_0, Item_1)
			end
			-- END ARRAY
			-- Buffer restore
			local Result = buffer.create(SendCursor)
			buffer.copy(Result, 0, SendBuffer, 0, SendCursor)
			Load(Previous)
			return Result
		end
	},
	LargeRecord = {
		Read = function(Buffer: buffer): (LargeRecord)
			-- Buffer overwrite
			local PreviousCursor = RecieveCursor
			local PreviousBuffer = RecieveBuffer
			local PreviousInstanceCursor = RecieveInstanceCursor
			RecieveCursor = 0
			RecieveBuffer = Buffer
			-- Read BLOCK: 2 bytes
			local BLOCK_START = Read(2)
			-- Buffer read
			local Value = {} :: any
			Value.entries = {}
			local Elements_1 = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
			for _ = 1, Elements_1 do
				local OFFSET_0 = Read(2)
				local Length = buffer.readu16(RecieveBuffer, OFFSET_0)
				local Key_1 = buffer.readstring(RecieveBuffer, Read(Length), Length)
				local OFFSET_1 = Read(1)
				local Element_1 = buffer.readu8(RecieveBuffer, OFFSET_1)
				Value.entries[Key_1] = Element_1
			end
			-- Buffer restore
			RecieveCursor = PreviousCursor
			RecieveBuffer = PreviousBuffer
			RecieveInstanceCursor = PreviousInstanceCursor
			return Value
		end,
		Write = function(Value: LargeRecord): (buffer)
			-- Buffer overwrite
			local Previous = Save()
			Load()
			-- Allocate BLOCK: 2 bytes
			local BLOCK_START = Allocate(2)
			-- Buffer write
			local Elements_1 = 0
			for Key_1, Element_1 in Value.entries do
				Elements_1 += 1
				local Length = #Key_1
				local OFFSET_0 = Allocate(2)
				buffer.writeu16(SendBuffer, OFFSET_0, Length)
				Allocate(Length)
				buffer.writestring(SendBuffer, SendOffset, Key_1, Length)
				local OFFSET_1 = Allocate(1)
				buffer.writeu8(SendBuffer, OFFSET_1, Element_1)
			end
			buffer.writeu16(SendBuffer, BLOCK_START + 0, Elements_1)
			-- Buffer restore
			local Result = buffer.create(SendCursor)
			buffer.copy(Result, 0, SendBuffer, 0, SendCursor)
			Load(Previous)
			return Result
		end
	},
	StringHeavy = {
		Read = function(Buffer: buffer): (StringHeavy)
			-- Buffer overwrite
			local PreviousCursor = RecieveCursor
			local PreviousBuffer = RecieveBuffer
			local PreviousInstanceCursor = RecieveInstanceCursor
			RecieveCursor = 0
			RecieveBuffer = Buffer
			-- Read BLOCK: 6 bytes
			local BLOCK_START = Read(6)
			-- Buffer read
			local Value = {} :: any
			local Length = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
			Value.title = buffer.readstring(RecieveBuffer, Read(Length), Length)
			Length = buffer.readu16(RecieveBuffer, BLOCK_START + 2)
			Value.author = buffer.readstring(RecieveBuffer, Read(Length), Length)
			-- START ARRAY
			Length = buffer.readu16(RecieveBuffer, BLOCK_START + 4)
			if Length < 0 then error(`Expected "Length" to be larger than 0, got {Length} instead.`) end
			if Length > 65535 then error(`Expected "Length" to be smaller than 65535, got {Length} instead.`) end
			Value.lines = table.create(Length)
				-- Read BLOCK: 2 bytes
			local ARRAY_START_1 = Read(2 * Length)
			for Index = 1, Length do
				-- Read 2
				local OPERATION_OFFSET_0 = ARRAY_START_1
				ARRAY_START_1 += 2
				Length = buffer.readu16(RecieveBuffer, OPERATION_OFFSET_0)
				local Item_1 = buffer.readstring(RecieveBuffer, Read(Length), Length)
				table.insert(Value.lines, Item_1)
			end
			-- END ARRAY
			-- Buffer restore
			RecieveCursor = PreviousCursor
			RecieveBuffer = PreviousBuffer
			RecieveInstanceCursor = PreviousInstanceCursor
			return Value
		end,
		Write = function(Value: StringHeavy): (buffer)
			-- Buffer overwrite
			local Previous = Save()
			Load()
			-- Allocate BLOCK: 6 bytes
			local BLOCK_START = Allocate(6)
			-- Buffer write
			local Length = #Value.title
			buffer.writeu16(SendBuffer, BLOCK_START + 0, Length)
			Allocate(Length)
			buffer.writestring(SendBuffer, SendOffset, Value.title, Length)
			local Length = #Value.author
			buffer.writeu16(SendBuffer, BLOCK_START + 2, Length)
			Allocate(Length)
			buffer.writestring(SendBuffer, SendOffset, Value.author, Length)
			-- START ARRAY
			buffer.writeu16(SendBuffer, BLOCK_START + 4, #Value.lines)
				-- Allocate BLOCK: 2 bytes
			local ARRAY_START_1 = Allocate(2 * #Value.lines)
			for Index = 1, #Value.lines do
				local Item_1 = Value.lines[Index]
				local Length = #Item_1
				-- Allocate 2
				local OPERATION_OFFSET_0 = ARRAY_START_1
				ARRAY_START_1 += 2
				buffer.writeu16(SendBuffer, OPERATION_OFFSET_0, Length)
				Allocate(Length)
				buffer.writestring(SendBuffer, SendOffset, Item_1, Length)
			end
			-- END ARRAY
			-- Buffer restore
			local Result = buffer.create(SendCursor)
			buffer.copy(Result, 0, SendBuffer, 0, SendCursor)
			Load(Previous)
			return Result
		end
	},
	TaggedUnion = {
		Read = function(Buffer: buffer): (TaggedUnion)
			-- Buffer overwrite
			local PreviousCursor = RecieveCursor
			local PreviousBuffer = RecieveBuffer
			local PreviousInstanceCursor = RecieveInstanceCursor
			RecieveCursor = 0
			RecieveBuffer = Buffer
			-- Read BLOCK: 2 bytes
			local BLOCK_START = Read(2)
			-- Buffer read
			local Value = {} :: any
			-- START ARRAY
			local Length = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
			if Length < 0 then error(`Expected "Length" to be larger than 0, got {Length} instead.`) end
			if Length > 65535 then error(`Expected "Length" to be smaller than 65535, got {Length} instead.`) end
			Value.events = table.create(Length)
				-- Read BLOCK: 1 bytes
			local ARRAY_START_1 = Read(1 * Length)
			for Index = 1, Length do
				-- Read 1
				local OPERATION_OFFSET_0 = ARRAY_START_1
				ARRAY_START_1 += 1
				local Tag = buffer.readu8(RecieveBuffer, OPERATION_OFFSET_0)
				local Item_1 = true :: any
				if Tag == 0 then
					-- Read BLOCK: 16 bytes
					local BLOCK_START = Read(16)
					Item_1 = {} :: any
					Item_1.id = buffer.readu32(RecieveBuffer, BLOCK_START + 0)
					local X = buffer.readf32(RecieveBuffer, BLOCK_START + 4)
					local Y = buffer.readf32(RecieveBuffer, BLOCK_START + 8)
					local Z = buffer.readf32(RecieveBuffer, BLOCK_START + 12)
					Item_1.at = Vector3.new(X, Y, Z)
					Item_1.kind = "spawn"
				elseif Tag == 1 then
					-- Read BLOCK: 6 bytes
					local BLOCK_START = Read(6)
					Item_1 = {} :: any
					Item_1.id = buffer.readu32(RecieveBuffer, BLOCK_START + 0)
					Item_1.amount = buffer.readu16(RecieveBuffer, BLOCK_START + 4)
					Item_1.kind = "damage"
				elseif Tag == 2 then
					-- Read BLOCK: 6 bytes
					local BLOCK_START = Read(6)
					Item_1 = {} :: any
					Item_1.id = buffer.readu32(RecieveBuffer, BLOCK_START + 0)
					Length = buffer.readu16(RecieveBuffer, BLOCK_START + 4)
					Item_1.text = buffer.readstring(RecieveBuffer, Read(Length), Length)
					Item_1.kind = "chat"
				elseif Tag == 3 then
					-- Read BLOCK: 4 bytes
					local BLOCK_START = Read(4)
					Item_1 = {} :: any
					Item_1.id = buffer.readu32(RecieveBuffer, BLOCK_START + 0)
					Item_1.kind = "despawn"
				else
					error(`Unexpected variant: {Tag}`)
				end
				table.insert(Value.events, Item_1)
			end
			-- END ARRAY
			-- Buffer restore
			RecieveCursor = PreviousCursor
			RecieveBuffer = PreviousBuffer
			RecieveInstanceCursor = PreviousInstanceCursor
			return Value
		end,
		Write = function(Value: TaggedUnion): (buffer)
			-- Buffer overwrite
			local Previous = Save()
			Load()
			-- Allocate BLOCK: 2 bytes
			local BLOCK_START = Allocate(2)
			-- Buffer write
			-- START ARRAY
			buffer.writeu16(SendBuffer, BLOCK_START + 0, #Value.events)
				-- Allocate BLOCK: 1 bytes
			local ARRAY_START_1 = Allocate(1 * #Value.events)
			for Index = 1, #Value.events do
				local Item_1 = Value.events[Index]
				-- Allocate 1
				local OPERATION_OFFSET_0 = ARRAY_START_1
				ARRAY_START_1 += 1
				if Item_1.kind == "spawn" then
					buffer.writeu8(SendBuffer, OPERATION_OFFSET_0, 0)
					-- Allocate BLOCK: 16 bytes
					local BLOCK_START = Allocate(16)
					buffer.writeu32(SendBuffer, BLOCK_START + 0, Item_1.id)
					local Vector = Item_1.at
					buffer.writef32(SendBuffer, BLOCK_START + 4, Vector.X)
					buffer.writef32(SendBuffer, BLOCK_START + 8, Vector.Y)
					buffer.writef32(SendBuffer, BLOCK_START + 12, Vector.Z)
				elseif Item_1.kind == "damage" then
					buffer.writeu8(SendBuffer, OPERATION_OFFSET_0, 1)
					-- Allocate BLOCK: 6 bytes
					local BLOCK_START = Allocate(6)
					buffer.writeu32(SendBuffer, BLOCK_START + 0, Item_1.id)
					buffer.writeu16(SendBuffer, BLOCK_START + 4, Item_1.amount)
				elseif Item_1.kind == "chat" then
					buffer.writeu8(SendBuffer, OPERATION_OFFSET_0, 2)
					-- Allocate BLOCK: 6 bytes
					local BLOCK_START = Allocate(6)
					buffer.writeu32(SendBuffer, BLOCK_START + 0, Item_1.id)
					local Length = #Item_1.text
					buffer.writeu16(SendBuffer, BLOCK_START + 4, Length)
					Allocate(Length)
					buffer.writestring(SendBuffer, SendOffset, Item_1.text, Length)
				elseif Item_1.kind == "despawn" then
					buffer.writeu8(SendBuffer, OPERATION_OFFSET_0, 3)
					-- Allocate BLOCK: 4 bytes
					local BLOCK_START = Allocate(4)
					buffer.writeu32(SendBuffer, BLOCK_START + 0, Item_1.id)
				else
					error(`Unexpected variant: {Item_1.kind}`)
				end
			end
			-- END ARRAY
			-- Buffer restore
			local Result = buffer.create(SendCursor)
			buffer.copy(Result, 0, SendBuffer, 0, SendCursor)
			Load(Previous)
			return Result
		end
	},
	Toggles = {
		Read = function(Buffer: buffer): (Toggles)
			-- Buffer overwrite
			local PreviousCursor = RecieveCursor
			local PreviousBuffer = RecieveBuffer
			local PreviousInstanceCursor = RecieveInstanceCursor
			RecieveCursor = 0
			RecieveBuffer = Buffer
			-- Read BLOCK: 13 bytes
			local BLOCK_START = Read(13)
			-- Buffer read
			local Value = {} :: any
			Value.a = (buffer.readu8(RecieveBuffer, BLOCK_START + 0) == 1)
			Value.b = (buffer.readu8(RecieveBuffer, BLOCK_START + 1) == 1)
			Value.c = (buffer.readu8(RecieveBuffer, BLOCK_START + 2) == 1)
			Value.d = (buffer.readu8(RecieveBuffer, BLOCK_START + 3) == 1)
			Value.e = (buffer.readu8(RecieveBuffer, BLOCK_START + 4) == 1)
			Value.f = (buffer.readu8(RecieveBuffer, BLOCK_START + 5) == 1)
			Value.g = (buffer.readu8(RecieveBuffer, BLOCK_START + 6) == 1)
			Value.h = (buffer.readu8(RecieveBuffer, BLOCK_START + 7) == 1)
			Value.i = (buffer.readu8(RecieveBuffer, BLOCK_START + 8) == 1)
			Value.j = (buffer.readu8(RecieveBuffer, BLOCK_START + 9) == 1)
			Value.level = buffer.readu8(RecieveBuffer, BLOCK_START + 10)
			if buffer.readu8(RecieveBuffer, BLOCK_START + 11) == 1 then
				-- Read BLOCK: 2 bytes
				local BLOCK_START = Read(2)
				local Length = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
				Value.label = buffer.readstring(RecieveBuffer, Read(Length), Length)
			end
			if buffer.readu8(RecieveBuffer, BLOCK_START + 12) == 1 then
				-- Read BLOCK: 2 bytes
				local BLOCK_START = Read(2)
				Value.offset = buffer.readi16(RecieveBuffer, BLOCK_START + 0)
			end
			-- Buffer restore
			RecieveCursor = PreviousCursor
			RecieveBuffer = PreviousBuffer
			RecieveInstanceCursor = PreviousInstanceCursor
			return Value
		end,
		Write = function(Value: Toggles): (buffer)
			-- Buffer overwrite
			local Previous = Save()
			Load()
			-- Allocate BLOCK: 13 bytes
			local BLOCK_START = Allocate(13)
			-- Buffer write
			buffer.writeu8(SendBuffer, BLOCK_START + 0, Value.a and 1 or 0)
			buffer.writeu8(SendBuffer, BLOCK_START + 1, Value.b and 1 or 0)
			buffer.writeu8(SendBuffer, BLOCK_START + 2, Value.c and 1 or 0)
			buffer.writeu8(SendBuffer, BLOCK_START + 3, Value.d and 1 or 0)
			buffer.writeu8(SendBuffer, BLOCK_START + 4, Value.e and 1 or 0)
			buffer.writeu8(SendBuffer, BLOCK_START + 5, Value.f and 1 or 0)
			buffer.writeu8(SendBuffer, BLOCK_START + 6, Value.g and 1 or 0)
			buffer.writeu8(SendBuffer, BLOCK_START + 7, Value.h and 1 or 0)
			buffer.writeu8(SendBuffer, BLOCK_START + 8, Value.i and 1 or 0)
			buffer.writeu8(SendBuffer, BLOCK_START + 9, Value.j and 1 or 0)
			buffer.writeu8(SendBuffer, BLOCK_START + 10, Value.level)
			buffer.writeu8(SendBuffer, BLOCK_START + 11, Value.label ~= nil and 1 or 0)
			if Value.label ~= nil then
				-- Allocate BLOCK: 2 bytes
				local BLOCK_START = Allocate(2)
				local Length = #Value.label
				buffer.writeu16(SendBuffer, BLOCK_START + 0, Length)
				Allocate(Length)
				buffer.writestring(SendBuffer, SendOffset, Value.label, Length)
			end
			buffer.writeu8(SendBuffer, BLOCK_START + 12, Value.offset ~= nil and 1 or 0)
			if Value.offset ~= nil then
				-- Allocate BLOCK: 2 bytes
				local BLOCK_START = Allocate(2)
				buffer.writei16(SendBuffer, BLOCK_START + 0, Value.offset)
			end
			-- Buffer restore
			local Result = buffer.create(SendCursor)
			buffer.copy(Result, 0, SendBuffer, 0, SendCursor)
			Load(Previous)
			return Result
		end
	},
	Transforms = {
		Read = function(Buffer: buffer): (Transforms)
			-- Buffer overwrite
			local PreviousCursor = RecieveCursor
			local PreviousBuffer = RecieveBuffer
			local PreviousInstanceCursor = RecieveInstanceCursor
			RecieveCursor = 0
			RecieveBuffer = Buffer
			-- Read BLOCK: 2 bytes
			local BLOCK_START = Read(2)
			-- Buffer read
			local Value = {} :: any
			-- START ARRAY
			local Length = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
			if Length < 0 then error(`Expected "Length" to be larger than 0, got {Length} instead.`) end
			if Length > 65535 then error(`Expected "Length" to be smaller than 65535, got {Length} instead.`) end
			Value.list = table.create(Length)
				-- Read BLOCK: 24 bytes
			local ARRAY_START_1 = Read(24 * Length)
			for Index = 1, Length do
				-- Read 4
				local OPERATION_OFFSET_0 = ARRAY_START_1
				ARRAY_START_1 += 4
				local X = buffer.readf32(RecieveBuffer, OPERATION_OFFSET_0)
				-- Read 4
				local OPERATION_OFFSET_1 = ARRAY_START_1
				ARRAY_START_1 += 4
				local Y = buffer.readf32(RecieveBuffer, OPERATION_OFFSET_1)
				-- Read 4
				local OPERATION_OFFSET_2 = ARRAY_START_1
				ARRAY_START_1 += 4
				local Z = buffer.readf32(RecieveBuffer, OPERATION_OFFSET_2)
				local Position = Vector3.new(X, Y, Z)
				-- Read 4
				local OPERATION_OFFSET_3 = ARRAY_START_1
				ARRAY_START_1 += 4
				local rX = buffer.readf32(RecieveBuffer, OPERATION_OFFSET_3)
				-- Read 4
				local OPERATION_OFFSET_4 = ARRAY_START_1
				ARRAY_START_1 += 4
				local rY = buffer.readf32(RecieveBuffer, OPERATION_OFFSET_4)
				-- Read 4
				local OPERATION_OFFSET_5 = ARRAY_START_1
				ARRAY_START_1 += 4
				local rZ = buffer.readf32(RecieveBuffer, OPERATION_OFFSET_5)
				local Item_1 = CFrame.new(Position) * CFrame.fromOrientation(rX, rY, rZ)
				table.insert(Value.list, Item_1)
			end
			-- END ARRAY
			-- Buffer restore
			RecieveCursor = PreviousCursor
			RecieveBuffer = PreviousBuffer
			RecieveInstanceCursor = PreviousInstanceCursor
			return Value
		end,
		Write = function(Value: Transforms): (buffer)
			-- Buffer overwrite
			local Previous = Save()
			Load()
			-- Allocate BLOCK: 2 bytes
			local BLOCK_START = Allocate(2)
			-- Buffer write
			-- START ARRAY
			buffer.writeu16(SendBuffer, BLOCK_START + 0, #Value.list)
				-- Allocate BLOCK: 24 bytes
			local ARRAY_START_1 = Allocate(24 * #Value.list)
			for Index = 1, #Value.list do
				local Item_1 = Value.list[Index]
				local Vector = Item_1.Position
				-- Allocate 4
				local OPERATION_OFFSET_0 = ARRAY_START_1
				ARRAY_START_1 += 4
				buffer.writef32(SendBuffer, OPERATION_OFFSET_0, Vector.X)
				-- Allocate 4
				local OPERATION_OFFSET_1 = ARRAY_START_1
				ARRAY_START_1 += 4
				buffer.writef32(SendBuffer, OPERATION_OFFSET_1, Vector.Y)
				-- Allocate 4
				local OPERATION_OFFSET_2 = ARRAY_START_1
				ARRAY_START_1 += 4
				buffer.writef32(SendBuffer, OPERATION_OFFSET_2, Vector.Z)
				local rX, rY, rZ = Item_1:ToOrientation()
				-- Allocate 4
				local OPERATION_OFFSET_3 = ARRAY_START_1
				ARRAY_START_1 += 4
				buffer.writef32(SendBuffer, OPERATION_OFFSET_3, rX)
				-- Allocate 4
				local OPERATION_OFFSET_4 = ARRAY_START_1
				ARRAY_START_1 += 4
				buffer.writef32(SendBuffer, OPERATION_OFFSET_4, rY)
				-- Allocate 4
				local OPERATION_OFFSET_5 = ARRAY_START_1
				ARRAY_START_1 += 4
				buffer.writef32(SendBuffer, OPERATION_OFFSET_5, rZ)
			end
			-- END ARRAY
			-- Buffer restore
			local Result = buffer.create(SendCursor)
			buffer.copy(Result, 0, SendBuffer, 0, SendCursor)
			Load(Previous)
			return Result
		end
	},
	Booleans = {
		Read = function(Buffer: buffer): (Booleans)
			-- Buffer overwrite
			local PreviousCursor = RecieveCursor
			local PreviousBuffer = RecieveBuffer
			local PreviousInstanceCursor = RecieveInstanceCursor
			RecieveCursor = 0
			RecieveBuffer = Buffer
			-- Read BLOCK: 2 bytes
			local BLOCK_START = Read(2)
			-- Buffer read
			local Value = {} :: any
			-- START ARRAY
			local Length = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
			if Length < 0 then error(`Expected "Length" to be larger than 0, got {Length} instead.`) end
			if Length > 1000 then error(`Expected "Length" to be smaller than 1000, got {Length} instead.`) end
			Value.values = table.create(Length)
				-- Read BLOCK: 1 bytes
			local ARRAY_START_1 = Read(1 * Length)
			for Index = 1, Length do
				-- Read 1
				local OPERATION_OFFSET_0 = ARRAY_START_1
				ARRAY_START_1 += 1
				local Item_1 = (buffer.readu8(RecieveBuffer, OPERATION_OFFSET_0) == 1)
				table.insert(Value.values, Item_1)
			end
			-- END ARRAY
			-- Buffer restore
			RecieveCursor = PreviousCursor
			RecieveBuffer = PreviousBuffer
			RecieveInstanceCursor = PreviousInstanceCursor
			return Value
		end,
		Write = function(Value: Booleans): (buffer)
			-- Buffer overwrite
			local Previous = Save()
			Load()
			-- Allocate BLOCK: 2 bytes
			local BLOCK_START = Allocate(2)
			-- Buffer write
			-- START ARRAY
			buffer.writeu16(SendBuffer, BLOCK_START + 0, #Value.values)
				-- Allocate BLOCK: 1 bytes
			local ARRAY_START_1 = Allocate(1 * #Value.values)
			for Index = 1, #Value.values do
				local Item_1 = Value.values[Index]
				-- Allocate 1
				local OPERATION_OFFSET_0 = ARRAY_START_1
				ARRAY_START_1 += 1
				buffer.writeu8(SendBuffer, OPERATION_OFFSET_0, Item_1 and 1 or 0)
			end
			-- END ARRAY
			-- Buffer restore
			local Result = buffer.create(SendCursor)
			buffer.copy(Result, 0, SendBuffer, 0, SendCursor)
			Load(Previous)
			return Result
		end
	},
	Entities = {
		Read = function(Buffer: buffer): (Entities)
			-- Buffer overwrite
			local PreviousCursor = RecieveCursor
			local PreviousBuffer = RecieveBuffer
			local PreviousInstanceCursor = RecieveInstanceCursor
			RecieveCursor = 0
			RecieveBuffer = Buffer
			-- Read BLOCK: 2 bytes
			local BLOCK_START = Read(2)
			-- Buffer read
			local Value = {} :: any
			-- START ARRAY
			local Length = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
			if Length < 0 then error(`Expected "Length" to be larger than 0, got {Length} instead.`) end
			if Length > 1000 then error(`Expected "Length" to be smaller than 1000, got {Length} instead.`) end
			Value.entities = table.create(Length)
				-- Read BLOCK: 6 bytes
			local ARRAY_START_1 = Read(6 * Length)
			for Index = 1, Length do
				local Item_1 = {} :: any
				-- Read 1
				local OPERATION_OFFSET_0 = ARRAY_START_1
				ARRAY_START_1 += 1
				Item_1.a = buffer.readu8(RecieveBuffer, OPERATION_OFFSET_0)
				-- Read 1
				local OPERATION_OFFSET_1 = ARRAY_START_1
				ARRAY_START_1 += 1
				Item_1.b = buffer.readu8(RecieveBuffer, OPERATION_OFFSET_1)
				-- Read 1
				local OPERATION_OFFSET_2 = ARRAY_START_1
				ARRAY_START_1 += 1
				Item_1.c = buffer.readu8(RecieveBuffer, OPERATION_OFFSET_2)
				-- Read 1
				local OPERATION_OFFSET_3 = ARRAY_START_1
				ARRAY_START_1 += 1
				Item_1.d = buffer.readu8(RecieveBuffer, OPERATION_OFFSET_3)
				-- Read 1
				local OPERATION_OFFSET_4 = ARRAY_START_1
				ARRAY_START_1 += 1
				Item_1.e = buffer.readu8(RecieveBuffer, OPERATION_OFFSET_4)
				-- Read 1
				local OPERATION_OFFSET_5 = ARRAY_START_1
				ARRAY_START_1 += 1
				Item_1.f = buffer.readu8(RecieveBuffer, OPERATION_OFFSET_5)
				table.insert(Value.entities, Item_1)
			end
			-- END ARRAY
			-- Buffer restore
			RecieveCursor = PreviousCursor
			RecieveBuffer = PreviousBuffer
			RecieveInstanceCursor = PreviousInstanceCursor
			return Value
		end,
		Write = function(Value: Entities): (buffer)
			-- Buffer overwrite
			local Previous = Save()
			Load()
			-- Allocate BLOCK: 2 bytes
			local BLOCK_START = Allocate(2)
			-- Buffer write
			-- START ARRAY
			buffer.writeu16(SendBuffer, BLOCK_START + 0, #Value.entities)
				-- Allocate BLOCK: 6 bytes
			local ARRAY_START_1 = Allocate(6 * #Value.entities)
			for Index = 1, #Value.entities do
				local Item_1 = Value.entities[Index]
				-- Allocate 1
				local OPERATION_OFFSET_0 = ARRAY_START_1
				ARRAY_START_1 += 1
				buffer.writeu8(SendBuffer, OPERATION_OFFSET_0, Item_1.a)
				-- Allocate 1
				local OPERATION_OFFSET_1 = ARRAY_START_1
				ARRAY_START_1 += 1
				buffer.writeu8(SendBuffer, OPERATION_OFFSET_1, Item_1.b)
				-- Allocate 1
				local OPERATION_OFFSET_2 = ARRAY_START_1
				ARRAY_START_1 += 1
				buffer.writeu8(SendBuffer, OPERATION_OFFSET_2, Item_1.c)
				-- Allocate 1
				local OPERATION_OFFSET_3 = ARRAY_START_1
				ARRAY_START_1 += 1
				buffer.writeu8(SendBuffer, OPERATION_OFFSET_3, Item_1.d)
				-- Allocate 1
				local OPERATION_OFFSET_4 = ARRAY_START_1
				ARRAY_START_1 += 1
				buffer.writeu8(SendBuffer, OPERATION_OFFSET_4, Item_1.e)
				-- Allocate 1
				local OPERATION_OFFSET_5 = ARRAY_START_1
				ARRAY_START_1 += 1
				buffer.writeu8(SendBuffer, OPERATION_OFFSET_5, Item_1.f)
			end
			-- END ARRAY
			-- Buffer restore
			local Result = buffer.create(SendCursor)
			buffer.copy(Result, 0, SendBuffer, 0, SendCursor)
			Load(Previous)
			return Result
		end
	},
})
type BLINK_EVENTS_SYMBOL = typeof(Returns)
return Returns :: BLINK_EVENTS_SYMBOL